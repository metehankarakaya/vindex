class PinConstants {
  PinConstants._();

  // Shake animation
  static const Duration shakeAnimationDuration = Duration(milliseconds: 500);
  static const double shakeOffset = 12.0;

  // Dot fill animation
  static const Duration dotAnimationDuration = Duration(milliseconds: 200);

  // PIN dot dimensions
  static const double dotSize = 20.0;
  static const double dotMargin = 10.0;
  static const double dotBorderWidth = 2.0;

  // Layout spacing
  static const double screenPadding = 16.0;
  static const double topSpacing = 100.0;
  static const double topSpacingSetup = 50.0;
  static const double titleSubtitleGap = 12.0;
  static const double subtitleDotsGap = 48.0;
  static const double dotsKeypadGap = 60.0;

  // Keypad grid
  static const int gridColumns = 3;
  static const double gridAspectRatio = 1.2;
  static const double gridSpacing = 10.0;
  static const double backspaceIconSize = 28.0;
}
